/**
 * OCR 프롬프트 - [지문]/[/지문] 태그 기반 구조화된 출력
 * past-exam-web에서 포팅됨
 */

const CIRCLE_CHAR_RULES = `⚠️ 최우선 지시사항 - 원문자 필수 유지:
원문자는 선택지 번호로 사용되는 필수 문자야. 원문자를 일반 숫자나 알파벳으로 변환하거나 제거하면 안 돼.
이미지에 원문자가 보이면 반드시 그대로 추출해줘.

⚠️ 매우 중요한 구분:
- 원문자 알파벳: ⓐ, ⓑ, ⓒ, ⓓ, ⓔ, ⓕ, ⓖ, ⓗ, ⓘ, ⓙ (이것들은 절대 변환하면 안 돼!)
- 원문자 숫자: ㉠, ㉡, ㉢, ㉣, ㉤ (이것들도 절대 변환하면 안 돼!)
- 원문자 숫자(선택지): ①, ②, ③, ④, ⑤, ⑥, ⑦, ⑧, ⑨, ⑩ (이것들도 절대 변환하면 안 돼!)

⚠️ 절대 금지 사항:
- 원문자 알파벳(ⓐ, ⓑ 등)을 원문자 숫자(㉠, ㉡ 등)로 변환하면 안 돼!
- 원문자 숫자(㉠, ㉡ 등)를 원문자 알파벳(ⓐ, ⓑ 등)으로 변환하면 안 돼!
- 이미지에 ⓐ가 보이면 반드시 ⓐ로 추출하고, ㉠로 변환하면 안 돼!
- 이미지에 ㉠가 보이면 반드시 ㉠로 추출하고, ⓐ로 변환하면 안 돼!`;

const TAG_RULES = `📌 출력 태그 규칙 (매우 중요):

1. [지문]...[/지문] 태그: 지문(본문·제시문·읽기 자료)을 감싸는 태그.
   ※ 지문 = 문제를 풀기 위해 읽어야 하는 본문 텍스트 (영어 지문, 한글 지문, 제시문, 빈칸이 있는 문장 등)
   ※ 문제 지시문("다음 글의 요지로 가장 적절한 것은?" 등)은 [지문] 밖에!
   ※ 선택지(①②③④⑤)도 [지문] 밖에!

2. [문제] 태그: 각 문제의 시작 표시.
   - [문제] 바로 다음에 문제 번호와 내용을 같은 줄에 이어서 작성해줘.
   - 이미지에 보이는 문제 번호(1., 2., 21. 등)는 반드시 그대로 보존해줘.

3. [N번 ~M번] 또는 [N~M]: 한 지문에 여러 문제가 있을 때 공유 헤더.

📌 출력 구조 (5가지 케이스):
① 한 지문+여러 문제 → [N~M] 공유 헤더 → [지문]...[/지문] → [문제]×N
② 독립 문제(지문 있음) → [문제] 헤더 → [지문]...[/지문] → 선택지
③ 독립 문제(지문 없음) → [문제] 헤더 → 선택지
④ 지문만(문제 없음) → [N~M] 공유 헤더 → [지문]...[/지문]
⑤ 문제 지시문이 보이지 않는 경우 → 보이는 텍스트를 그대로 추출

⚠️ 가장 중요한 원칙: 이미지에 텍스트가 있으면 반드시 추출해줘.

정답과 해설은 추출하지 마. 텍스트만 추출해줘.
점수 표시([2.8점], [3.1점] 등)가 있으면 그대로 포함해줘.`;

const FEW_SHOT_EXAMPLES = `예시:
---
[1번 ~2번] 다음 글을 읽고 질문에 답하시오.

[지문]
Hurricanes form only over ocean water that has a temperature of 26 or more degrees Celsius.
[/지문]

[문제] 1. 윗글의 제목으로 가장 적절한 것은? [2.8점]
① Ways to Prepare for Hurricane
② Circulation of Water and Wind
③ Different Names for Hurricanes
④ Steps in Hurricane Development
⑤ Regional Factors of Hurricane Formation
---`;

const FINAL_CHECK = `⚠️ 최종 확인:
- 원문자(①②③ⓐⓑⓒ㉠㉡㉢ 등)는 절대 변환하지 마.
- 지문은 반드시 [지문]...[/지문]으로 감싸줘.
- 문제 지시문과 선택지는 [지문] 태그 밖에 둬줘.
- 이미지에 있는 모든 텍스트를 빠짐없이 추출해줘.
- 절대 빈 응답을 반환하지 마!`;

export const EXTRACT_TEXT_PROMPT = `이 이미지는 시험지 문제야. 이미지에 있는 모든 텍스트를 정확하게 추출해줘.

${CIRCLE_CHAR_RULES}

중요한 지시사항:
1. 원문자는 반드시 그대로 유지해줘. 절대 제거하거나 변환하지 마.
2. 일반 텍스트는 그대로 추출해줘. 밑줄이나 볼드 등 서식은 무시하고 순수 텍스트만 추출해줘.
3. 원문에 충실하게 OCR해줘. LaTeX 변환 금지!
4. 한글, 영어, 숫자, 기호 등 모든 문자를 빠짐없이 추출해줘.

${TAG_RULES}

${FEW_SHOT_EXAMPLES}

${FINAL_CHECK}`;

export const EXTRACT_TEXT_WITH_FORMATTING_PROMPT = `이 이미지는 시험지 문제야. 이미지에 있는 모든 텍스트를 정확하게 추출하되, 서식도 함께 표시해줘.

${CIRCLE_CHAR_RULES}

중요한 지시사항:
1. 원문자는 반드시 그대로 유지해줘.
2. **밑줄 서식**: 밑줄이 있는 텍스트는 **텍스트** 형식으로 감싸줘.
3. ***볼드 서식***: 볼드(두꺼운) 텍스트는 ***텍스트*** 형식으로 감싸줘.
4. ##밑줄+볼드 서식##: 밑줄과 볼드가 동시에 적용된 텍스트는 ##텍스트## 형식으로 감싸줘.
5. 표가 있다면 <table>내용</table> 형식으로 감싸줘.

${TAG_RULES}

${FEW_SHOT_EXAMPLES}

${FINAL_CHECK}`;

export const EXTRACT_MULTIPLE_REGIONS_PROMPT = `이 이미지들은 시험지 문제의 여러 부분이야. 모든 이미지에 있는 텍스트를 순서대로 정확하게 추출해줘.

${CIRCLE_CHAR_RULES}

중요한 지시사항:
1. 원문자는 반드시 그대로 유지해줘.
2. 일반 텍스트는 그대로 추출. 서식은 무시.
3. 여러 이미지가 제공되면, 각 이미지의 내용을 순서대로 이어서 하나의 연속된 텍스트로 만들어줘.

${TAG_RULES}

${FEW_SHOT_EXAMPLES}

${FINAL_CHECK}`;

export const EXTRACT_MULTIPLE_REGIONS_WITH_FORMATTING_PROMPT = `이 이미지들은 시험지 문제의 여러 부분이야. 모든 이미지에 있는 텍스트를 순서대로 정확하게 추출하되, 서식도 함께 표시해줘.

${CIRCLE_CHAR_RULES}

중요한 지시사항:
1. 원문자는 반드시 그대로 유지해줘.
2. **밑줄**, ***볼드***, ##밑줄+볼드## 서식 표시
3. 표가 있다면 <table>내용</table> 형식
4. 여러 이미지가 제공되면, 순서대로 이어서 하나의 연속된 텍스트로 만들어줘.

${TAG_RULES}

${FEW_SHOT_EXAMPLES}

${FINAL_CHECK}`;

export const FALLBACK_SIMPLE_PROMPT = `이 이미지에 있는 모든 텍스트를 정확하게 추출해줘.

규칙:
1. 이미지에 보이는 모든 텍스트를 빠짐없이 그대로 추출해줘.
2. 원문자(①②③④⑤ⓐⓑⓒⓓⓔ㉠㉡㉢㉣㉤ 등)는 반드시 그대로 유지해줘.
3. 본문/지문이 있으면 [지문]...[/지문] 태그로 감싸줘.
4. 형식에 맞지 않더라도 빈 응답 대신 텍스트를 반드시 출력해줘.`;

export const ANSWER_EXPLANATION_PROMPT = (text: string) =>
  `다음은 시험지 문제 텍스트야. 이 텍스트를 분석해서 정답과 해설을 추가해줘.

⚠️ 중요 지시사항:
1. 원문자는 절대 변경하지 마.
2. 원본 텍스트의 모든 내용을 그대로 유지해줘.
3. [지문]...[/지문] 태그가 있으면 그대로 유지해줘.
4. 각 문제 뒤에 [정답]과 [해설]을 추가해줘.

출력 형식:
- 원본 문제 텍스트를 그대로 유지
- 각 문제 뒤에 [정답] (정답 번호 또는 내용) 형식으로 정답 추가
- [정답] 다음에 [해설] (해설 내용) 형식으로 해설 추가

다음 텍스트를 처리해줘:

${text}`;

export const BATCH_ANSWER_PROMPT = (texts: string[]) => {
  const combined = texts
    .map((t, i) => `--- 문제 ${i + 1} ---\n${t}`)
    .join('\n\n');

  return `다음은 여러 시험 문제들이야. 각 문제에 정답과 해설을 추가해줘.

⚠️ 중요 지시사항:
1. 원문자는 절대 변경하지 마.
2. 원본 텍스트의 모든 내용을 그대로 유지해줘.
3. 각 문제의 구분자(--- 문제 N ---)를 반드시 그대로 유지해줘.
4. 각 문제 뒤에 [정답]과 [해설]을 추가해줘.

다음 문제들을 처리해줘:

${combined}`;
};

export const EXAM_ANALYSIS_PROMPT = (jsonText: string, examName: string) =>
  `너는 영어/국어 시험 분석 전문가야. 다음 JSON 데이터는 "${examName}" 시험지에서 추출한 문제들이야.

⚠️ 분석 지시사항:
1. 모든 문제를 꼼꼼히 읽고 유형을 분류해줘
2. 각 문제의 난이도를 상/중/하로 판단해줘
3. 킬러문항(고난도 변별력 문항)을 찾아줘
4. 시험 전체의 경향성과 특징을 분석해줘
5. 학습 계획을 구체적으로 제안해줘

⚠️ 출력 형식:
반드시 아래 JSON 형식으로만 응답해줘.

{
  "summary": {
    "total_questions": (총 문항 수),
    "overall_difficulty": "(상/중상/중/중하/하)",
    "exam_name": "${examName}",
    "trend_analysis": "${examName}는 ... (반드시 시험 이름으로 시작해서 전체적인 경향성과 특징을 서술)"
  },
  "type_classification": [
    {
      "type": "(유형명)",
      "count": (해당 유형 문항 수),
      "question_numbers": [(해당 문항 번호들)],
      "difficulty_avg": "(상/중/하)"
    }
  ],
  "difficulty_distribution": {
    "high": (상 문항 수),
    "mid": (중 문항 수),
    "low": (하 문항 수)
  },
  "killer_questions": [
    {
      "q_number": (문항 번호),
      "type": "(문제 유형)",
      "reason": "(킬러문항인 이유)",
      "strategy": "(풀기 위한 전략)"
    }
  ],
  "study_plan": [
    {
      "priority": (1부터 순서대로),
      "area": "(학습 영역)",
      "content": "(구체적인 학습 방법)",
      "target_types": ["(관련 문제 유형들)"]
    }
  ]
}

분석할 시험지 JSON 데이터:

${jsonText}`;
